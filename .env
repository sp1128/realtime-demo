# 架构是三段级联：火山流式ASR → OpenAI流式LLM → 火山流式TTS。
# 三段里只有 LLM 走代理，语音那两段是国内直连——绕出境会白搭几百毫秒。
# 配完先跑 npm run check-proxy，它会分别体检这两条路。

# ============ 必填 ============

# 火山引擎语音。控制台 console.volcengine.com →「语音技术」里拿，ASR 和 TTS 共用一对
VOLC_APP_ID=2017995201
VOLC_ACCESS_TOKEN=gDwuOiQkS_emDKKVWW52ddGUIpvML-6U

# OpenAI，只用来跑 LLM
OPENAI_API_KEY=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

# 直连超时的话，取消下面这行注释，端口改成你自己代理的端口（走 WebSocket，HTTP 代理能正常转发）
HTTPS_PROXY=http://127.0.0.1:7897

# ============ 火山资源号 ============
# 要跟你实际开通的套餐对上，填错会在 WebSocket 握手阶段就被拒（401/403）。
# 报错时先怀疑这两行。

# 流式语音识别：小时版 volc.bigasr.sauc.duration / 并发版 volc.bigasr.sauc.concurrent
# VOLC_ASR_RESOURCE_ID=volc.bigasr.sauc.duration

# 语音合成：双向流式是 10029。注意单向流式是另一个号，两者不通用
# VOLC_TTS_RESOURCE_ID=volc.service_type.10029


# ============ 音色与语速 ============
# 音色 ID 在控制台「音色列表」里复制。默认这个是男声，
# 电话销售场景建议试几个再定，音色对信任感的影响比话术还大
VOLC_TTS_SPEAKER=S_DPrV4x0d2

# 语速偏移，0 是原速，负数更慢更沉稳。范围大致 -50 ~ 100
# VOLC_TTS_SPEECH_RATE=-5


# ============ LLM ============
# 换模型只改这一行。人设遵循度不够就往上换，首字太慢就往下换
# LLM_MODEL=gpt-4o-mini
# 单轮输出上限。兜底闸门，主要还得靠提示词约束。300 大约够 2~3 句中文
# MAX_OUTPUT_TOKENS=220
# 越高越活泛，越低越守话术。电话销售 0.7~0.9 之间比较合适
# LLM_TEMPERATURE=0.8
# 保留最近几轮对话。越长首 token 越慢，电话不需要记住二十分钟前的细节
# HISTORY_TURNS=12

# 提示词整个换掉可以用这个（不填就用 lib/prompt.js 里那份）。
# 平时直接改 lib/prompt.js 更方便，这里适合临时切一套话术做 A/B
# AGENT_INSTRUCTIONS=


# ============ 轮次判断 ============
# 静音多久算客户说完。小了客户喘口气就被抢白，大了接话发木
# VAD_SILENCE_MS=500

# 判定"有人在说话"需要超过噪声底的多少倍。
# 噪声底是自己长出来的（空调、风扇这类稳态噪声会被自动吸收），一般不用动。
# 环境嘈杂、AI 老是被杂音触发时往上调到 3.5~5
# VAD_RATIO=3.0

# 静音之后再等一下才提交，让 ASR 最后一段结果落地。
# 调小会丢掉客户最后两三个字，调大接话变慢，250 是比较稳的起点
# COMMIT_DELAY_MS=250


# ============ 打断 ============
# 走的是「确认式打断」：VAD 一响先按住播放、停止给 TTS 喂新句子，
# 但不立刻销毁——等 ASR 真吐出字来才确认。这样咳嗽、纸响、
# AI 自己从声卡回环漏回来的声音都打不断它。

# 最多等多久去确认。超时没听到字就当误报，把话接着说完
# BARGE_CONFIRM_MS=1000
# 要听到几个字才算数。设 1 容易被回声里的单字误触
# BARGE_MIN_CHARS=2


# ============ 转写调优 ============
# 通话里会出现的人名、公司名、行话、缩写，逗号分隔。
# 这个对准确率的提升最直接，值得花时间填
# ASR_HOTWORDS=张伟,李娜,报价单,交期,佐藤商事


# ============ 开场 ============
# 接通后由 AI 先开口（外呼场景需要）。填 false 则等客户先说话
# AUTO_GREET=true
# 等一下再开口，避免开场头两个字在声卡还没准备好时被切掉
# GREET_DELAY_MS=400


# ============ 代理 ============
# 只作用在 LLM 那一跳。火山是国内直连，代码里不给它挂 agent。
# Clash 默认 7890，V2Ray 默认 10809，具体端口看你自己客户端的设置。
# 代理要认证的话写成 http://用户名:密码@127.0.0.1:7890
#
# ⚠️ 注意别开系统级全局代理：那会把火山的流量也绕出境，
# 延迟从 20ms 变成 300ms，而且完全不报错，只是每句话都慢。
# npm run check-proxy 会把这种情况直接指出来。
# HTTPS_PROXY=http://127.0.0.1:7890
